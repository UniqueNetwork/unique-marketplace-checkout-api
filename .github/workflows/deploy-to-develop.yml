name: Deploy to Develop

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    name: Build dockers
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        working-directory: .
        run: docker build -t checkout-market .docker

